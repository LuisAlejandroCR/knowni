# eligibility.signals.txt: the public signal order, taken from the compiler.
# Generated by `circom eligibility.circom --sym` and NOT written by hand: it is
# the order the Groth16 verifier consumes, and the contract asserts against it.
# Regenerate with the command in circuits/README.md after touching the circuit.
personhood
solvencyTier
formality
standing
nullifier
issuerRoot
sessionId
expectedSubjectRef
rentMinor
nowMonth
maxStaleMonths
minMonthsPaid
listSetRoot
