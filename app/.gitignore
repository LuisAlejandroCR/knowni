node_modules/
.expo/
.expo-export/

# Generated by `expo prebuild` / `expo run:*` from app.json (continuous native
# generation): regenerated on every native build, never edited or committed.
/android/
/ios/
